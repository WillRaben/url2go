<%= form_with(model: long_uri, local: true) do |form| %>
  <% if long_uri.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(long_uri.errors.count, "error") %> prohibited this long_uri from being saved:</h2>

      <ul>
      <% long_uri.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :org_url %>
    <%= form.text_area :org_url %>
  </div>

  <div class="field">
    <%= form.label :short_url %>
    <%= form.text_field :short_url %>
  </div>

  <div class="field">
    <%= form.label :protocol_id %>
    <%= form.number_field :protocol_id %>
  </div>

  <div class="field">
    <%= form.label :hits %>
    <%= form.text_field :hits %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
