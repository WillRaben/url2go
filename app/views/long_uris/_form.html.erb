<%= form_with(model: long_uri, local: true) do |form| %>
  <% if long_uri.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(long_uri.errors.count, "error") %> prohibited this long_uri from being saved:</h2>

      <ul>
      <% long_uri.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :org_url, class: "form-control"%>
  </div>

  <div class="actions">
    <%= form.submit 'Get Short URL' , class: "btn btn-dark"%>
  </div>
<% end %>
